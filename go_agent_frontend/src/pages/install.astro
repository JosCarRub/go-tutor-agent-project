---
import Layout from '../layouts/Layout.astro';
import HeroInstall from '../install/sections/HeroInstall.astro';
import LocalAdvantages from '../install/sections/LocalAdvantages.astro';
import InstallGuide from '../install/sections/InstallGuide.astro';

---

<Layout title="Instalación Local - Go Tutor Agent">
  
  <HeroInstall />
  <LocalAdvantages />
  <InstallGuide />


</Layout>

<style is:global>
  .terminal-window-guide {
    background-color: #1e1e1e; border-radius: 0.75rem;
    box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5);
    border: 1px solid rgba(255,255,255,0.1);
  }
  .terminal-header {
    padding: 0.75rem; display: flex; align-items: center;
    border-bottom: 1px solid rgba(255,255,255,0.1);
  }

  .terminal-title { color: #9ca3af; font-size: 0.875rem; margin: 0 auto; }
  .terminal-body {
    padding: 1rem 1.5rem; font-family: 'SF Mono', 'Fira Code', 'Menlo', monospace;
    font-size: 0.875rem; line-height: 1.75; color: #d4d4d4;
    white-space: pre-wrap;
  }
  .line { display: flex; align-items: center; margin-bottom: 0.25rem; }
  .prompt-symbol { color: #6ee7b7; margin-right: 0.75rem; }
  .comment { color: #6a9955; font-style: italic; }
  .command { color: #d4d4d4; flex-grow: 1; }
  .copy-btn {
    background: none; border: none; color: #6b7280; cursor: pointer;
    opacity: 0; transition: opacity 0.2s; margin-left: 0.5rem;
  }
  .line:hover .copy-btn { opacity: 1; }
  .copy-btn:hover { color: #d4d4d4; }
  @keyframes blink { 50% { opacity: 0; } }
  .blink { animation: blink 1s step-end infinite; }
  code {
    background-color: rgba(105, 214, 228, 0.1);
    color: var(--go-color);
    padding: 0.125rem 0.375rem;
    border-radius: 0.25rem;
    font-size: 0.875em;
    font-weight: 500;
  }
</style>