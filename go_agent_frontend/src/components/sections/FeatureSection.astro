---
// src/components/sections/FeatureSection.astro
import Card from '../ui/Card.astro';
---
<section class="w-full max-w-5xl mx-auto py-12 sm:py-16 px-4">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
    
    <!-- Card 1: Análisis con RAG -->
    <Card variant="feature" padding="md" rounded="xl">
      <div class="feature-content">
        <div class="feature-icon">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4" /></svg>
        </div>
        <h3 class="text-xl font-semibold text-[var(--text-primary)]">Análisis con RAG</h3>
        <p class="text-sm text-[var(--text-secondary)] mt-2 flex-grow">
          Respuestas enriquecidas con la documentación más actual para una precisión inigualable.
        </p>
      </div>
    </Card>

    <!-- Card 2: Sandbox de Código -->
    <Card variant="feature" padding="md" rounded="xl">
      <div class="feature-content">
        <div class="feature-icon">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" /></svg>
        </div>
        <h3 class="text-xl font-semibold text-[var(--text-primary)]">Sandbox de Código</h3>
        <p class="text-sm text-[var(--text-secondary)] mt-2 flex-grow">
          Ejecuta y prueba tu código de Go en un entorno seguro y aislado directamente en el chat.
        </p>
      </div>
    </Card>

    <!-- Card 3: Experto en Concurrencia -->
    <Card variant="feature" padding="md" rounded="xl">
      <div class="feature-content">
        <div class="feature-icon">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M14 10h4.764a2 2 0 011.789 2.894l-3.5 7A2 2 0 0115.263 21h-4.017c-.163 0-.326-.02-.485-.06L7 20m7-10V5a2 2 0 00-2-2h-.095c-.5 0-.905.405-.905.905 0 .714-.211 1.412-.608 2.006L7 11v9m7-10h-2M7 20H5a2 2 0 01-2-2v-6a2 2 0 012-2h2.5" /></svg>
        </div>
        <h3 class="text-xl font-semibold text-[var(--text-primary)]">Experto en Concurrencia</h3>
        <p class="text-sm text-[var(--text-secondary)] mt-2 flex-grow">
          Domina goroutines y channels con explicaciones claras y ejemplos prácticos.
        </p>
      </div>
    </Card>

  </div>
</section>

<style is:global>
  .feature-content {
    position: relative;
    z-index: 1;
    padding: 2rem;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .feature-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 4rem;
    height: 4rem;
    border-radius: 50%;
    margin-bottom: 1.5rem;
    color: var(--go-color-dark);
    background-color: var(--go-color);
    border: 1px solid var(--border);
    transition: all 0.3s ease;
    box-shadow: 0 4px 12px color-mix(in srgb, var(--go-color) 20%, transparent);
  }

  .feature-card-base:hover .feature-icon {
    transform: scale(1.1);
  }
</style>